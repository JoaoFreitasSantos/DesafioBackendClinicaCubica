const express = require('express')
const routes = express.Router();

// TODO: Registrar os endpoints de maneira simples; deixar regra de negocio para controller.
const RegraController = require('./controllers/RegraController');
